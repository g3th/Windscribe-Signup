### Account Maker for Windscribe ###

Not functional. Hopefully solving captcha with OCR one day...

---------------------------------------------------------------

### Requires: ##

Tesseract:

```sudo apt install tesseract-ocr```

Chromedriver:

```sudo apt-get install chromium-driver```

Pytesseract library:

```pip3 install pytesseract```
